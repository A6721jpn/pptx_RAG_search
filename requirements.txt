# Core dependencies for local PPTX RAG POC

# PPTX processing
python-pptx>=0.6.23
pywin32>=306  # Windows COM automation (PowerPoint rendering)

# PDF processing
pdfplumber>=0.10.0  # PDF text extraction
pdf2image>=1.16.3   # PDF to image conversion (requires poppler)
# Note: pdf2image requires poppler-utils (Windows: https://github.com/oschwartz10612/poppler-windows/releases/)

# Embeddings (CPU-friendly)
sentence-transformers>=2.2.2  # Text embeddings
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0  # Image processing

# Vector database
qdrant-client>=1.7.0

# Utilities
pyyaml>=6.0.1
numpy>=1.24.0

# Development tools (optional)
pytest>=7.4.0
black>=23.0.0
