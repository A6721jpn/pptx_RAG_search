# Core dependencies for PPTX RAG system

# PPTX processing
python-pptx>=0.6.23
pywin32>=306  # Windows COM automation (PowerPoint rendering)

# SharePoint integration
msgraph-sdk>=1.0.0
azure-identity>=1.15.0
aiohttp>=3.9.0
aiofiles>=23.2.1

# Embeddings
sentence-transformers>=2.2.2
open-clip-torch>=2.23.0
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0

# Vector database
qdrant-client>=1.7.0

# Utilities
pyyaml>=6.0.1
tenacity>=8.2.3
python-dotenv>=1.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Logging & monitoring
structlog>=23.2.0
prometheus-client>=0.19.0  # Optional: for metrics

# Task scheduling (optional)
celery>=5.3.0  # Optional: for scheduled jobs
redis>=5.0.0   # Optional: Celery broker

# Development tools
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# CLI
click>=8.1.0
rich>=13.0.0  # Optional: for better CLI output
